interface:
  display_name: "Codex Luna Swarm"
  short_description: "Launch and collect independent Luna lanes"
  default_prompt: "Use $codex-luna-swarm to verify the named inputs, prepare distinct bounded lanes, and return the launch receipt; wait and collect reports only when the request asks for them."
