{
  "name": "codex-luna-swarm",
  "version": "0.1.0",
  "private": true,
  "description": "Repo-scoped Codex skill for launching independent Luna subagents.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.cjs test/*.test.mjs",
    "validate-skill": "node scripts/validate-skill.mjs"
  }
}
