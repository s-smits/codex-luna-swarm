interface:
  display_name: "Codex Luna Swarm"
  short_description: "Launch and collect independent Luna lanes"
  default_prompt: "Use $codex-luna-swarm to verify the named inputs first, then launch every requested Luna lane with a distinct, exhaustive, self-contained brief, remain active through the terminal event, and collect the reports."
